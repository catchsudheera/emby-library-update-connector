{
  "name": "emby-library-update-connector-tests",
  "version": "1.0.0",
  "description": "Integration tests for emby-library-update-connector",
  "scripts": {
    "test": "jest tests/integration",
    "test:java": "TEST_BASE_URL=http://localhost:8080 jest tests/integration",
    "test:node": "TEST_BASE_URL=http://localhost:3000 jest tests/integration"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}
